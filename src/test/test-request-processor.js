const config = require('../config/config.js');
const log = config.getLog();
const assert = require('assert');

const processor = require('../auth-process.js');

module.exports = processorTest;

function processorTest() {
    // processor(request, reply);
}